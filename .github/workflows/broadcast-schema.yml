name: Broadcast Schema to Spokes

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  generate:
    name: Generate Types
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.get-repos.outputs.repos }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Generate TypeScript Types
        run: npx supabase gen types typescript --project-id ${{ secrets.SUPABASE_PROJECT_ID }} > types/database.types.ts
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      
      - name: Format Types
        run: node scripts/format-types.js
      
      - name: Commit to Hub
        run: |
          git config user.name "EryAI Schema Bot"
          git config user.email "bot@eryai.tech"
          git add types/
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update database types from supabase" && git push)
      
      - name: Get EryAI repos
        id: get-repos
        run: |
          REPOS=$(curl -s -H "Authorization: token ${{ secrets.GH_TOKEN }}" \
            "https://api.github.com/users/shabajeric91-sketch/repos?per_page=100" | \
            jq -r '[.[] | select(.name | test("^eryai|^ery-ai")) | select(.name != "eryai-core-schema") | .name] | @json')
          echo "repos=$REPOS" >> $GITHUB_OUTPUT
          echo "Found repos: $REPOS"
      
      - name: Upload types artifact
        uses: actions/upload-artifact@v4
        with:
          name: generated-types
          path: types/

  broadcast:
    name: Push to ${{ matrix.repo }}
    needs: generate
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: ${{ fromJson(needs.generate.outputs.repos) }}
      fail-fast: false
    
    steps:
      - name: Download types artifact
        uses: actions/download-artifact@v4
        with:
          name: generated-types
          path: types/
      
      - name: Create CI workflow file
        run: |
          mkdir -p .github/workflows
          cat > .github/workflows/ci.yml << 'EOF'
          name: CI Validation

          on:
            push:
              branches: [main]
            pull_request:
              branches: [main]

          jobs:
            validate:
              name: Type Check & Build
              runs-on: ubuntu-latest
              
              steps:
                - uses: actions/checkout@v4
                
                - name: Setup Node.js
                  uses: actions/setup-node@v4
                  with:
                    node-version: '20'
                
                - name: Install dependencies
                  run: npm install
                
                - name: Run Type Check
                  run: |
                    npx tsc --noEmit > type-errors.log 2>&1 || {
                      echo "### ❌ Type Check Failed" >> $GITHUB_STEP_SUMMARY
                      echo "" >> $GITHUB_STEP_SUMMARY
                      echo "Copy this error to Claude:" >> $GITHUB_STEP_SUMMARY
                      echo '```' >> $GITHUB_STEP_SUMMARY
                      cat type-errors.log >> $GITHUB_STEP_SUMMARY
                      echo '```' >> $GITHUB_STEP_SUMMARY
                      exit 1
                    }
                    echo "### ✅ Type Check Passed" >> $GITHUB_STEP_SUMMARY
                
                - name: Build
                  run: npm run build
          EOF
      
      - name: Push to ${{ matrix.repo }}
        run: |
          echo "🚀 Pushing to ${{ matrix.repo }}..."
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/shabajeric91-sketch/${{ matrix.repo }}.git spoke-repo
          
          mkdir -p spoke-repo/types
          mkdir -p spoke-repo/.github/workflows
          
          cp types/database.types.ts spoke-repo/types/database.types.ts
          cp .github/workflows/ci.yml spoke-repo/.github/workflows/ci.yml
          
          cd spoke-repo
          git config user.name "EryAI Schema Bot"
          git config user.email "bot@eryai.tech"
          git add types/ .github/
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: sync schema and CI from eryai-core-schema" && git push)
          echo "✅ Done with ${{ matrix.repo }}"
